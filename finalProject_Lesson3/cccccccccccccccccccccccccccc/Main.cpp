#include"Camera.h"
#include"Simulator.h"
#include"Buffer.h"
#include<thread>
#include<iostream>
#include"StatusMessage.h"
int main() {
	Simulator*s=new Simulator();
	//
	//char** b = c->sendBufferToServer()->getBuffer();
	//if (b)
	//	std::cout << "buffer in index" << "=" << b;
	//return 1;
}
